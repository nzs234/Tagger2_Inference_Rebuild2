# Source manifest for lock generation; deployments install a requirements-*.lock.
# ML dependencies stay in the CPU/GPU manifests so timm cannot select the wrong
# PyTorch wheel before the runtime variant is known.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
pydantic>=2.7,<3
pydantic-settings>=2.4,<3
httpx>=0.27,<1
Pillow>=10,<13
numpy>=1.24,<3
huggingface_hub>=0.24,<2
safetensors>=0.4,<1
transformers>=4.47,<6
keyring>=25,<26
# Adapter controls are part of the shipped local-inference workflow. Torch is
# installed from the selected CPU/CUDA lock before these packages are resolved.
peft>=0.13,<1
lycoris-lora>=3,<4
